import 'package:flutter/material.dart';
import 'package:flutter_dialog/pagesatu.dart';

class MainApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: PageSatu(),
    );
  }
}
